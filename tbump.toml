github_url = "https://github.com/maniaclab/voms-token-service"

[version]
current = "0.2.1"

regex = '''
  (?P<major>\d+)
  \.
  (?P<minor>\d+)
  \.
  (?P<patch>\d+)
  (rc
    (?P<candidate>\d+)
  )?
  '''

[git]
message_template = "chore: bump version 0.2.1 → {new_version}"
tag_template = "v{new_version}"

[[file]]
src = "tbump.toml"
search = "bump version {current_version} → "

[[file]]
src = "pixi.toml"
# Match only the workspace-level version line, not dependency version constraints.
search = 'version = "{current_version}"'

[[file]]
src = "pyproject.toml"
search = 'version = "{current_version}"'

[[file]]
src = "src/voms_token_service/app.py"
search = 'version="{current_version}"'

[[file]]
src = "charts/voms-token-service/Chart.yaml"
search = 'appVersion: "{current_version}"'

[[file]]
src = "charts/voms-token-service/Chart.yaml"
search = 'version: {current_version}'

[[field]]
name = "candidate"
default = ""
